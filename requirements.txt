# P1 - AI Passenger Experience Assistant
# Tüm bileşenler ücretsiz / yerel çalışacak şekilde seçildi (bkz. README ve p1_proje_plani.md)

# --- Web servisi ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# --- LLM (Ollama'nın OpenAI-uyumlu API'si üzerinden) ---
openai==1.51.0

# --- Agent orkestrasyon ---
langgraph==0.2.34
langchain==0.3.1
langchain-community==0.3.1

# --- RAG: embeddings + vector db ---
sentence-transformers==3.1.1
qdrant-client==1.11.2
rank_bm25==0.2.2  # yalnızca ADR-0005'in hibrit arama deneyi için (evaluation/hybrid_search_experiment.py) — REDDEDİLDİ, üretimde kullanılmıyor

# --- Intent / fine-tuning ---
transformers==4.45.1
datasets==3.0.1
torch==2.4.1
accelerate==1.14.0
scikit-learn==1.5.2

# --- Klasik ML (P2 ile paylaşılan, Adım 4 EDA/segment analizi için) ---
pandas==2.2.3
numpy==1.26.4

# --- Deney takibi (Adım 4) ---
mlflow==3.14.0

# --- Guardrails / şema doğrulama ---
jsonschema==4.23.0

# --- Observability ---
langfuse==2.51.3
prometheus-client==0.21.0

# --- Test / CI ---
pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2
